name: Deploy to CPanel

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Deploy Files to CPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          # port: 22

          # GitHub repo-la backend folder
          local-dir: ./backend/

          # CPanel Application Root
          server-dir: /

          # .env upload aaga koodathu
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/.env
            **/node_modules/**
            **/README.md
      # - name: Restart Node.js App via SSH
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.FTP_SERVER }}
      #     username: ${{ secrets.SSH_USER }}
      #     password: ${{ secrets.SSH_PASSWORD }}
      #     port: 22
      #     script: |
      #       cd /home2/amigoweb/nextshow_backend_v2/
      #       # --prefer-offline speed-a velai mudikkum
      #       npm install --prefer-offline --no-audit
      #       # Node.js App-ai restart panna signal
      #       touch tmp/restart.txt
